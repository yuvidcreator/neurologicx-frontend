Display the NeurologicX design system reference in a concise, copy-paste-ready format.

No arguments needed. Read `CLAUDE.md` and output a condensed cheat sheet:

1. **Color palette** — Tailwind class → use case (one line each)
2. **Gradient strings** — labeled copy-paste strings
3. **Typography classes** — labeled by usage (hero, section, body, etc.)
4. **Layout primitives** — section wrapper, card, buttons as TSX snippets
5. **Icon usage** — remind: Lucide React only, `w-5 h-5` or `w-6 h-6` standard sizes

Keep the output short. This is a quick-reference card, not documentation.
